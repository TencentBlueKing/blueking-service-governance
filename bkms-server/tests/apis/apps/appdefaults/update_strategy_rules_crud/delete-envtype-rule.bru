meta {
  name: delete-envtype-strategy-rule
  type: http
  seq: 4
}

delete {
  url: {{serviceURL}}/workspaces/{{appDefaultsWorkspaceID}}/app-spec/update-strategy/{{envTypeStrategyRuleID}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

tests {
  test("should delete the envType strategy rule", () => {
    expect(res.status).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 10000
}
