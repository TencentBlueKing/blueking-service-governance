meta {
  name: delete-envtype-rule
  type: http
  seq: 4
}

delete {
  url: {{serviceURL}}/workspaces/{{appDefaultsWorkspaceID}}/app-spec/resources/{{envTypeResourcesRuleID}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

tests {
  test("should delete the envType rule", () => {
    expect(res.status).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 10000
}
